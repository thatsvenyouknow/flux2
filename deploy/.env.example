# Cloudflare Tunnel token (from: Zero Trust > Networks > Connectors > flux2-demo)
TUNNEL_TOKEN=your-tunnel-token-here

# Host path to model weights directory
# This is bind-mounted read-only into the container at /data/image_models
MODEL_PATH=/data/image_models
